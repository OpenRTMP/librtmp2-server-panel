# Bug scan configuration

## SCAN_TARGET_OVERRIDE

Leave empty to follow the module rotation in `bug-scan-progress.md`.
Set to a module path to force-scan that module for a single run.

```
SCAN_TARGET_OVERRIDE=
```
